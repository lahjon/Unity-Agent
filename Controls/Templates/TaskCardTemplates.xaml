<ResourceDictionary x:Class="Spritely.Controls.Templates.TaskCardTemplates"
                    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
        <!-- ═══ ACTIVE TASK ═══ -->
        <DataTemplate x:Key="ActiveTaskTemplate">
            <Border x:Name="Card" CornerRadius="8" Padding="10"
                    Margin="{Binding NestingDepth, Converter={StaticResource NestingDepthToMargin}}"
                    BorderThickness="1"
                    PreviewMouseDown="TaskCard_PreviewMiddleDown"
                    MouseUp="TaskCard_MouseUp"
                    PreviewMouseLeftButtonDown="TaskCard_PreviewMouseDown"
                    MouseMove="TaskCard_MouseMove"
                    AllowDrop="True"
                    DragOver="TaskCard_DragOver"
                    Drop="TaskCard_Drop"
                    >
                <Border.ContextMenu>
                    <ContextMenu>
                        <MenuItem Header="Continue Recommendations" Click="ContinueTask_Click"
                                  Visibility="{Binding IsContinuable, Converter={StaticResource BoolToVisibility}}"/>
                        <MenuItem Header="Copy Prompt" Click="CopyPrompt_Click"/>
                        <MenuItem Header="Export Output" Click="ExportTask_Click"/>
                        <MenuItem Header="View Output" Click="ViewOutput_Click"
                                  Visibility="{Binding IsFinished, Converter={StaticResource BoolToVisibility}}"/>
                        <MenuItem Header="Revert Changes" Click="RevertTask_Click"/>
                        <Separator/>
                        <MenuItem Header="Clone Task" Click="CloneTask_Click"/>
                        <MenuItem Header="Retry Task" Click="RetryTask_Click"/>
                        <MenuItem Header="Store Task" Click="StoreHistoryTask_Click"
                                  Visibility="{Binding IsFinished, Converter={StaticResource BoolToVisibility}}"/>
                    </ContextMenu>
                </Border.ContextMenu>
                <Border.Background>
                    <SolidColorBrush x:Name="CardBg" Color="#222222"/>
                </Border.Background>
                <Border.BorderBrush>
                    <SolidColorBrush x:Name="CardBorder" Color="#2C2C2C"/>
                </Border.BorderBrush>
                <Border.Triggers>
                    <EventTrigger RoutedEvent="MouseEnter">
                        <BeginStoryboard>
                            <Storyboard>
                                <ColorAnimation Storyboard.TargetName="CardBg" Storyboard.TargetProperty="Color"
                                                To="#2A2A2A" Duration="0:0:0.15"/>
                                <ColorAnimation Storyboard.TargetName="CardBorder" Storyboard.TargetProperty="Color"
                                                To="#3A3A3A" Duration="0:0:0.15"/>
                            </Storyboard>
                        </BeginStoryboard>
                    </EventTrigger>
                    <EventTrigger RoutedEvent="MouseLeave">
                        <BeginStoryboard>
                            <Storyboard>
                                <ColorAnimation Storyboard.TargetName="CardBg" Storyboard.TargetProperty="Color"
                                                To="#222222" Duration="0:0:0.2"/>
                                <ColorAnimation Storyboard.TargetName="CardBorder" Storyboard.TargetProperty="Color"
                                                To="#2C2C2C" Duration="0:0:0.2"/>
                            </Storyboard>
                        </BeginStoryboard>
                    </EventTrigger>
                </Border.Triggers>
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <!-- Top-right buttons + task number -->
                    <StackPanel Grid.Row="0" HorizontalAlignment="Right" VerticalAlignment="Top"
                                Margin="0,-2,-2,0" Panel.ZIndex="1">
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <!-- Force-start button (visible on queued/init-queued tasks) -->
                        <Button Click="ForceStartQueued_Click" Margin="0,0,2,0">
                            <Button.Style>
                                <Style TargetType="Button" BasedOn="{StaticResource IconBtn}">
                                    <Setter Property="Content" Value="&#xE768;"/>
                                    <Setter Property="Foreground" Value="#FFD600"/>
                                    <Setter Property="ToolTip" Value="Force start (bypass queue)"/>
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsQueued}" Value="True">
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding IsInitQueued}" Value="True">
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                        </Button>
                        <!-- Pause/Resume button -->
                        <Button Click="Pause_Click" Margin="0,0,2,0">
                            <Button.Style>
                                <Style TargetType="Button" BasedOn="{StaticResource IconBtn}">
                                    <Setter Property="Content" Value="&#xE769;"/>
                                    <Setter Property="Foreground" Value="{StaticResource Accent}"/>
                                    <Setter Property="ToolTip" Value="Pause task"/>
                                    <Setter Property="Visibility" Value="Visible"/>
                                    <Style.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Foreground" Value="{StaticResource AccentHover}"/>
                                        </Trigger>
                                        <DataTrigger Binding="{Binding IsPaused}" Value="True">
                                            <Setter Property="Content" Value="&#xE768;"/>
                                            <Setter Property="Foreground" Value="{StaticResource AccentHover}"/>
                                            <Setter Property="ToolTip" Value="Resume task"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding IsFinished}" Value="True">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding IsQueued}" Value="True">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding IsInitQueued}" Value="True">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding IsPlanning}" Value="True">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding IsSoftStopping}" Value="True">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                        </Button>
                        <!-- Soft-stop button (visible only when running/planning) -->
                        <Button Click="SoftStop_Click" Margin="0,0,2,0">
                            <Button.Style>
                                <Style TargetType="Button" BasedOn="{StaticResource IconBtn}">
                                    <Setter Property="Content" Value="&#xE71A;"/>
                                    <Setter Property="Foreground" Value="#FF8A65"/>
                                    <Setter Property="ToolTip" Value="Stop task gracefully"/>
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsRunning}" Value="True">
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding IsPlanning}" Value="True">
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </DataTrigger>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Foreground" Value="#FFAB91"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                        </Button>
                        <!-- Verify button (visible when finished) -->
                        <Button Click="VerifyTask_Click" Margin="0,0,2,0" FontSize="11">
                            <Button.Style>
                                <Style TargetType="Button" BasedOn="{StaticResource IconBtn}">
                                    <Setter Property="Content" Value="&#xE721;"/>
                                    <Setter Property="ToolTip" Value="Run result verification"/>
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsFinished}" Value="True">
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                        </Button>
                        <!-- Commit/Uncommit button (visible when finished) -->
                        <Button Click="CommitTask_Click" Margin="0,0,2,0" FontSize="11">
                            <Button.Style>
                                <Style TargetType="Button" BasedOn="{StaticResource IconBtn}">
                                    <Setter Property="Content" Value="&#xE74B;"/>
                                    <Setter Property="ToolTip" Value="Commit task changes"/>
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsFinished}" Value="True">
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding IsCommitted}" Value="True">
                                            <Setter Property="Content" Value="&#xE74D;"/>
                                            <Setter Property="Foreground" Value="{StaticResource Success}"/>
                                            <Setter Property="ToolTip" Value="Uncommit task changes"/>
                                        </DataTrigger>
                                        <!-- Pulse animation when uncommitted -->
                                        <DataTrigger Binding="{Binding IsCompletedUncommitted}" Value="True">
                                            <DataTrigger.EnterActions>
                                                <BeginStoryboard x:Name="CommitButtonPulseStoryboard">
                                                    <Storyboard RepeatBehavior="Forever">
                                                        <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                                                         From="0.6" To="1" Duration="0:0:0.8"
                                                                         AutoReverse="True"/>
                                                    </Storyboard>
                                                </BeginStoryboard>
                                            </DataTrigger.EnterActions>
                                            <DataTrigger.ExitActions>
                                                <StopStoryboard BeginStoryboardName="CommitButtonPulseStoryboard"/>
                                            </DataTrigger.ExitActions>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                        </Button>
                        <!-- Close/Remove button -->
                        <Button Click="Cancel_Click">
                            <Button.Style>
                                <Style TargetType="Button" BasedOn="{StaticResource IconBtn}">
                                    <Setter Property="Content" Value="&#xE8BB;"/>
                                    <Setter Property="ToolTip" Value="Cancel task"/>
                                    <Style.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Foreground" Value="{StaticResource DangerDeleteHover}"/>
                                        </Trigger>
                                        <DataTrigger Binding="{Binding IsFinished}" Value="True">
                                            <Setter Property="Content" Value="&#xE73E;"/>
                                            <Setter Property="Foreground" Value="{StaticResource Success}"/>
                                            <Setter Property="ToolTip" Value="Remove task"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                        </Button>
                    </StackPanel>
                    <TextBlock Text="{Binding HierarchyLabel}"
                               Foreground="{StaticResource TextSubdued}"
                               FontSize="10" FontFamily="Consolas" Margin="0,2,2,0"
                               HorizontalAlignment="Right"/>
                    </StackPanel>
                    <StackPanel Grid.Row="0">
                        <TextBlock Text="{Binding Id, StringFormat='ID: {0}'}"
                                   Foreground="{StaticResource TextMuted}"
                                   FontSize="9" FontFamily="Consolas" Margin="0,0,0,2"/>
                    <StackPanel Orientation="Horizontal">
                        <Grid Width="8" Height="8" Margin="0,0,6,0" VerticalAlignment="Center">
                            <Ellipse x:Name="StatusGlow" Width="8" Height="8"
                                     Fill="{Binding StatusColor, Converter={StaticResource StringToBrush}}">
                                <Ellipse.Style>
                                    <Style TargetType="Ellipse">
                                        <Setter Property="Effect">
                                            <Setter.Value>
                                                <BlurEffect Radius="0"/>
                                            </Setter.Value>
                                        </Setter>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsRunning}" Value="True">
                                                <DataTrigger.EnterActions>
                                                    <BeginStoryboard x:Name="GlowStoryboard">
                                                        <Storyboard RepeatBehavior="Forever">
                                                            <DoubleAnimation Storyboard.TargetProperty="(UIElement.Effect).(BlurEffect.Radius)"
                                                                             From="0" To="10" Duration="0:0:0.8"
                                                                             AutoReverse="True">
                                                                <DoubleAnimation.EasingFunction>
                                                                    <SineEase EasingMode="EaseInOut"/>
                                                                </DoubleAnimation.EasingFunction>
                                                            </DoubleAnimation>
                                                            <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                                                             From="0.3" To="1.0" Duration="0:0:0.8"
                                                                             AutoReverse="True">
                                                                <DoubleAnimation.EasingFunction>
                                                                    <SineEase EasingMode="EaseInOut"/>
                                                                </DoubleAnimation.EasingFunction>
                                                            </DoubleAnimation>
                                                        </Storyboard>
                                                    </BeginStoryboard>
                                                </DataTrigger.EnterActions>
                                                <DataTrigger.ExitActions>
                                                    <StopStoryboard BeginStoryboardName="GlowStoryboard"/>
                                                </DataTrigger.ExitActions>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding IsQueued}" Value="True">
                                                <DataTrigger.EnterActions>
                                                    <BeginStoryboard x:Name="QueuedGlowStoryboard">
                                                        <Storyboard RepeatBehavior="Forever">
                                                            <DoubleAnimation Storyboard.TargetProperty="(UIElement.Effect).(BlurEffect.Radius)"
                                                                             From="0" To="8" Duration="0:0:1.2"
                                                                             AutoReverse="True">
                                                                <DoubleAnimation.EasingFunction>
                                                                    <SineEase EasingMode="EaseInOut"/>
                                                                </DoubleAnimation.EasingFunction>
                                                            </DoubleAnimation>
                                                            <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                                                             From="0.4" To="1.0" Duration="0:0:1.2"
                                                                             AutoReverse="True">
                                                                <DoubleAnimation.EasingFunction>
                                                                    <SineEase EasingMode="EaseInOut"/>
                                                                </DoubleAnimation.EasingFunction>
                                                            </DoubleAnimation>
                                                        </Storyboard>
                                                    </BeginStoryboard>
                                                </DataTrigger.EnterActions>
                                                <DataTrigger.ExitActions>
                                                    <StopStoryboard BeginStoryboardName="QueuedGlowStoryboard"/>
                                                </DataTrigger.ExitActions>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding IsInitQueued}" Value="True">
                                                <DataTrigger.EnterActions>
                                                    <BeginStoryboard x:Name="InitQueuedGlowStoryboard">
                                                        <Storyboard RepeatBehavior="Forever">
                                                            <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                                                             From="0.3" To="1.0" Duration="0:0:1.5"
                                                                             AutoReverse="True">
                                                                <DoubleAnimation.EasingFunction>
                                                                    <SineEase EasingMode="EaseInOut"/>
                                                                </DoubleAnimation.EasingFunction>
                                                            </DoubleAnimation>
                                                        </Storyboard>
                                                    </BeginStoryboard>
                                                </DataTrigger.EnterActions>
                                                <DataTrigger.ExitActions>
                                                    <StopStoryboard BeginStoryboardName="InitQueuedGlowStoryboard"/>
                                                </DataTrigger.ExitActions>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding IsPaused}" Value="True">
                                                <DataTrigger.EnterActions>
                                                    <BeginStoryboard x:Name="PausedGlowStoryboard">
                                                        <Storyboard RepeatBehavior="Forever">
                                                            <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                                                             From="0.3" To="1.0" Duration="0:0:2.0"
                                                                             AutoReverse="True">
                                                                <DoubleAnimation.EasingFunction>
                                                                    <SineEase EasingMode="EaseInOut"/>
                                                                </DoubleAnimation.EasingFunction>
                                                            </DoubleAnimation>
                                                        </Storyboard>
                                                    </BeginStoryboard>
                                                </DataTrigger.EnterActions>
                                                <DataTrigger.ExitActions>
                                                    <StopStoryboard BeginStoryboardName="PausedGlowStoryboard"/>
                                                </DataTrigger.ExitActions>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding IsPlanning}" Value="True">
                                                <DataTrigger.EnterActions>
                                                    <BeginStoryboard x:Name="PlanningGlowStoryboard">
                                                        <Storyboard RepeatBehavior="Forever">
                                                            <DoubleAnimation Storyboard.TargetProperty="(UIElement.Effect).(BlurEffect.Radius)"
                                                                             From="0" To="8" Duration="0:0:1.0"
                                                                             AutoReverse="True">
                                                                <DoubleAnimation.EasingFunction>
                                                                    <SineEase EasingMode="EaseInOut"/>
                                                                </DoubleAnimation.EasingFunction>
                                                            </DoubleAnimation>
                                                            <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                                                             From="0.4" To="1.0" Duration="0:0:1.0"
                                                                             AutoReverse="True">
                                                                <DoubleAnimation.EasingFunction>
                                                                    <SineEase EasingMode="EaseInOut"/>
                                                                </DoubleAnimation.EasingFunction>
                                                            </DoubleAnimation>
                                                        </Storyboard>
                                                    </BeginStoryboard>
                                                </DataTrigger.EnterActions>
                                                <DataTrigger.ExitActions>
                                                    <StopStoryboard BeginStoryboardName="PlanningGlowStoryboard"/>
                                                </DataTrigger.ExitActions>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding IsCompletedUncommitted}" Value="True">
                                                <DataTrigger.EnterActions>
                                                    <BeginStoryboard x:Name="UncommittedPulseStoryboard">
                                                        <Storyboard RepeatBehavior="Forever">
                                                            <DoubleAnimation Storyboard.TargetProperty="Effect.Radius"
                                                                             From="0" To="10" Duration="0:0:1"
                                                                             AutoReverse="True"/>
                                                            <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                                                             From="0.7" To="1" Duration="0:0:1"
                                                                             AutoReverse="True"/>
                                                        </Storyboard>
                                                    </BeginStoryboard>
                                                </DataTrigger.EnterActions>
                                                <DataTrigger.ExitActions>
                                                    <StopStoryboard BeginStoryboardName="UncommittedPulseStoryboard"/>
                                                </DataTrigger.ExitActions>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding IsContinuable}" Value="True">
                                                <DataTrigger.EnterActions>
                                                    <BeginStoryboard x:Name="RecommendationPulseStoryboard">
                                                        <Storyboard RepeatBehavior="Forever">
                                                            <DoubleAnimation Storyboard.TargetProperty="(UIElement.Effect).(BlurEffect.Radius)"
                                                                             From="0" To="8" Duration="0:0:1.5"
                                                                             AutoReverse="True">
                                                                <DoubleAnimation.EasingFunction>
                                                                    <SineEase EasingMode="EaseInOut"/>
                                                                </DoubleAnimation.EasingFunction>
                                                            </DoubleAnimation>
                                                            <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                                                             From="0.5" To="1.0" Duration="0:0:1.5"
                                                                             AutoReverse="True">
                                                                <DoubleAnimation.EasingFunction>
                                                                    <SineEase EasingMode="EaseInOut"/>
                                                                </DoubleAnimation.EasingFunction>
                                                            </DoubleAnimation>
                                                        </Storyboard>
                                                    </BeginStoryboard>
                                                </DataTrigger.EnterActions>
                                                <DataTrigger.ExitActions>
                                                    <StopStoryboard BeginStoryboardName="RecommendationPulseStoryboard"/>
                                                </DataTrigger.ExitActions>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Ellipse.Style>
                            </Ellipse>
                            <Ellipse x:Name="StatusDot" Width="8" Height="8"
                                     Fill="{Binding StatusColor, Converter={StaticResource StringToBrush}}">
                                <Ellipse.Style>
                                    <Style TargetType="Ellipse">
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsCompletedUncommitted}" Value="True">
                                                <DataTrigger.EnterActions>
                                                    <BeginStoryboard x:Name="DotPulseStoryboard">
                                                        <Storyboard RepeatBehavior="Forever">
                                                            <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                                                             From="0.6" To="1" Duration="0:0:1.2"
                                                                             AutoReverse="True"/>
                                                        </Storyboard>
                                                    </BeginStoryboard>
                                                </DataTrigger.EnterActions>
                                                <DataTrigger.ExitActions>
                                                    <StopStoryboard BeginStoryboardName="DotPulseStoryboard"/>
                                                </DataTrigger.ExitActions>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Ellipse.Style>
                            </Ellipse>
                        </Grid>
                        <TextBlock Text="{Binding QueueStatusText}"
                                   Foreground="{Binding StatusColor, Converter={StaticResource StringToBrush}}"
                                   FontSize="11" FontWeight="SemiBold" FontFamily="Segoe UI"
                                   VerticalAlignment="Center">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Style.Triggers>
                                        <MultiDataTrigger>
                                            <MultiDataTrigger.Conditions>
                                                <Condition Binding="{Binding Path=IsQueued}" Value="True"/>
                                            </MultiDataTrigger.Conditions>
                                            <Setter Property="ToolTip">
                                                <Setter.Value>
                                                    <ToolTip MaxWidth="400" Placement="Top">
                                                        <TextBlock Text="{Binding QueuedTooltip}" TextWrapping="Wrap"/>
                                                    </ToolTip>
                                                </Setter.Value>
                                            </Setter>
                                        </MultiDataTrigger>
                                        <MultiDataTrigger>
                                            <MultiDataTrigger.Conditions>
                                                <Condition Binding="{Binding Path=IsInitQueued}" Value="True"/>
                                            </MultiDataTrigger.Conditions>
                                            <Setter Property="ToolTip">
                                                <Setter.Value>
                                                    <ToolTip MaxWidth="400" Placement="Top">
                                                        <TextBlock Text="{Binding QueuedTooltip}" TextWrapping="Wrap"/>
                                                    </ToolTip>
                                                </Setter.Value>
                                            </Setter>
                                        </MultiDataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                        <TextBlock Text=" | " Foreground="{StaticResource SeparatorDark}" FontSize="11" FontFamily="Segoe UI"
                                   VerticalAlignment="Center"/>
                        <TextBlock Text="{Binding ProjectName}" Foreground="{Binding ProjectColor, Converter={StaticResource StringToBrush}}"
                                   FontSize="11" FontFamily="Segoe UI" VerticalAlignment="Center"
                                   TextTrimming="CharacterEllipsis" MaxWidth="100"
                                   ToolTip="{Binding ProjectPath}"/>
                    </StackPanel>
                    </StackPanel>
                    <TextBlock Grid.Row="1" Text="{Binding ShortDescription}"
                               Foreground="{StaticResource TextLight}" FontSize="12" FontFamily="Segoe UI"
                               TextTrimming="CharacterEllipsis" Margin="0,4,0,0">
                        <TextBlock.ToolTip>
                            <ToolTip MaxWidth="600" Placement="Bottom">
                                <TextBlock Text="{Binding Description}" TextWrapping="Wrap" MaxWidth="580"/>
                            </ToolTip>
                        </TextBlock.ToolTip>
                    </TextBlock>
                    <!-- Tool activity feed + token usage -->
                    <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,4,0,0">
                        <StackPanel.Style>
                            <Style TargetType="StackPanel">
                                <Setter Property="Visibility" Value="Collapsed"/>
                                <Style.Triggers>
                                    <MultiDataTrigger>
                                        <MultiDataTrigger.Conditions>
                                            <Condition Binding="{Binding IsRunning}" Value="True"/>
                                            <Condition Binding="{Binding HasToolActivity}" Value="True"/>
                                        </MultiDataTrigger.Conditions>
                                        <Setter Property="Visibility" Value="Visible"/>
                                    </MultiDataTrigger>
                                    <DataTrigger Binding="{Binding HasTokenData}" Value="True">
                                        <Setter Property="Visibility" Value="Visible"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </StackPanel.Style>
                        <TextBlock Text="{Binding ToolActivityText}"
                                   Foreground="{StaticResource ToolActivityText}" FontSize="10" FontFamily="Consolas"
                                   TextTrimming="CharacterEllipsis" MaxWidth="200"
                                   LineHeight="15">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                    <Style.Triggers>
                                        <MultiDataTrigger>
                                            <MultiDataTrigger.Conditions>
                                                <Condition Binding="{Binding IsRunning}" Value="True"/>
                                                <Condition Binding="{Binding HasToolActivity}" Value="True"/>
                                            </MultiDataTrigger.Conditions>
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </MultiDataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                        <TextBlock Text=" | " Foreground="{StaticResource SeparatorDark}" FontSize="10" FontFamily="Consolas">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                    <Style.Triggers>
                                        <MultiDataTrigger>
                                            <MultiDataTrigger.Conditions>
                                                <Condition Binding="{Binding IsRunning}" Value="True"/>
                                                <Condition Binding="{Binding HasToolActivity}" Value="True"/>
                                                <Condition Binding="{Binding HasTokenData}" Value="True"/>
                                            </MultiDataTrigger.Conditions>
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </MultiDataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                        <TextBlock Text="{Binding TokenDisplayText}"
                                   Foreground="#8899AA" FontSize="10" FontFamily="Consolas"
                                   LineHeight="15">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding HasTokenData}" Value="True">
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                    </StackPanel>
                    <!-- Recommendations / Next Steps (visible when task has recommendations) -->
                    <TextBlock Grid.Row="3" Text="{Binding Recommendations}"
                               Foreground="#80CBC4" FontSize="10" FontFamily="Segoe UI"
                               TextWrapping="Wrap" TextTrimming="CharacterEllipsis"
                               MaxHeight="40" Margin="0,4,0,0">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Setter Property="Visibility" Value="Collapsed"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding HasRecommendations}" Value="True">
                                        <Setter Property="Visibility" Value="Visible"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                        <TextBlock.ToolTip>
                            <ToolTip MaxWidth="600" Placement="Bottom">
                                <TextBlock Text="{Binding Recommendations}" TextWrapping="Wrap" MaxWidth="580"
                                           Foreground="#B2DFDB"/>
                            </ToolTip>
                        </TextBlock.ToolTip>
                    </TextBlock>
                    <Grid Grid.Row="4" Margin="0,6,0,0">
                        <TextBlock Text="{Binding TimeInfo}" Foreground="{StaticResource TextMuted}"
                                   FontSize="10" VerticalAlignment="Center" FontFamily="Segoe UI"/>
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                            <Border CornerRadius="3" Padding="4,1" VerticalAlignment="Center"
                                    Background="{StaticResource PlanBadgeBg}" BorderBrush="{StaticResource PlanBadgeBorder}" BorderThickness="1">
                                <Border.Style>
                                    <Style TargetType="Border">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding PlanOnly}" Value="True">
                                                <Setter Property="Visibility" Value="Visible"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>
                                <TextBlock Text="PLAN" Foreground="{StaticResource PlanBadgeText}" FontSize="9"
                                           FontFamily="Segoe UI" FontWeight="Bold"/>
                            </Border>
                            <!-- Per-task toggle badges -->
                            <Border CornerRadius="3" Padding="4,1" Margin="4,0,0,0" VerticalAlignment="Center"
                                    Background="#33607D8B" BorderBrush="#55607D8B" BorderThickness="1"
                                    ToolTip="{Binding ActiveTogglesTooltip}">
                                <Border.Style>
                                    <Style TargetType="Border">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding HasActiveToggles}" Value="True">
                                                <Setter Property="Visibility" Value="Visible"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>
                                <TextBlock Text="{Binding ActiveTogglesText}" Foreground="#90A4AE" FontSize="9"
                                           FontFamily="Segoe UI" FontWeight="Bold"/>
                            </Border>
                            <!-- Priority badge -->
                            <Border CornerRadius="3" Padding="4,1" Margin="4,0,0,0" VerticalAlignment="Center"
                                    BorderThickness="1">
                                <Border.Style>
                                    <Style TargetType="Border">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                        <Setter Property="Background" Value="#33FFD600"/>
                                        <Setter Property="BorderBrush" Value="#55FFD600"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding HasPriorityBadge}" Value="True">
                                                <Setter Property="Visibility" Value="Visible"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding PriorityLevel}" Value="Critical">
                                                <Setter Property="Background" Value="#33FF5252"/>
                                                <Setter Property="BorderBrush" Value="#55FF5252"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding PriorityLevel}" Value="Low">
                                                <Setter Property="Background" Value="#3378909C"/>
                                                <Setter Property="BorderBrush" Value="#5578909C"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>
                                <TextBlock Text="{Binding PriorityBadgeText}"
                                           Foreground="{Binding PriorityBadgeColor, Converter={StaticResource StringToBrush}}"
                                           FontSize="9" FontFamily="Segoe UI" FontWeight="Bold"/>
                            </Border>
                            <!-- File lock padlock toggle -->
                            <Button Click="ToggleFileLock_Click" Margin="4,0,0,0" VerticalAlignment="Center">
                                <Button.Style>
                                    <Style TargetType="Button" BasedOn="{StaticResource IconBtn}">
                                        <Setter Property="Content" Value="&#xE72E;"/>
                                        <Setter Property="Foreground" Value="#64B5F6"/>
                                        <Setter Property="ToolTip" Value="File locks enforced — click to ignore"/>
                                        <Setter Property="FontSize" Value="11"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IgnoreFileLocks}" Value="True">
                                                <Setter Property="Content" Value="&#xE785;"/>
                                                <Setter Property="Foreground" Value="#666666"/>
                                                <Setter Property="ToolTip" Value="File locks ignored — click to enforce"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Button.Style>
                            </Button>
                            <Button Content="Continue" Click="ContinueTask_Click"
                                    Background="#FFB74D"
                                    Foreground="#1A1A1A"
                                    Margin="4,0,0,0"
                                    ToolTip="Continue task with recommended next steps">
                                <Button.Style>
                                    <Style TargetType="Button" BasedOn="{StaticResource SmallBtn}">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsContinuable}" Value="True">
                                                <Setter Property="Visibility" Value="Visible"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Button.Style>
                            </Button>
                        </StackPanel>
                    </Grid>
                </Grid>
            </Border>
        </DataTemplate>

        <!-- ═══ HISTORY TASK ═══ -->
        <DataTemplate x:Key="HistoryTaskTemplate">
            <Border CornerRadius="8" Padding="10" Margin="0,2"
                    BorderThickness="1"
                    >
                <Border.ContextMenu>
                    <ContextMenu>
                        <MenuItem Header="Resume" Click="Resume_Click"/>
                        <MenuItem Header="Continue Recommendations" Click="ContinueTask_Click"/>
                        <MenuItem Header="Re-run Task" Click="RerunTask_Click"/>
                        <MenuItem Header="Store Task" Click="StoreHistoryTask_Click"/>
                        <Separator/>
                        <MenuItem Header="Copy Prompt" Click="CopyPrompt_Click"/>
                        <MenuItem Header="Export Output" Click="ExportTask_Click"/>
                        <MenuItem Header="View Output" Click="ViewOutput_Click"/>
                        <Separator/>
                        <MenuItem Header="Revert Changes" Click="RevertTask_Click"/>
                    </ContextMenu>
                </Border.ContextMenu>
                <Border.Background>
                    <SolidColorBrush x:Name="HistCardBg" Color="#222222"/>
                </Border.Background>
                <Border.BorderBrush>
                    <SolidColorBrush x:Name="HistCardBorder" Color="#2C2C2C"/>
                </Border.BorderBrush>
                <Border.Triggers>
                    <EventTrigger RoutedEvent="MouseEnter">
                        <BeginStoryboard>
                            <Storyboard>
                                <ColorAnimation Storyboard.TargetName="HistCardBg" Storyboard.TargetProperty="Color"
                                                To="#282828" Duration="0:0:0.15"/>
                                <ColorAnimation Storyboard.TargetName="HistCardBorder" Storyboard.TargetProperty="Color"
                                                To="#383838" Duration="0:0:0.15"/>
                            </Storyboard>
                        </BeginStoryboard>
                    </EventTrigger>
                    <EventTrigger RoutedEvent="MouseLeave">
                        <BeginStoryboard>
                            <Storyboard>
                                <ColorAnimation Storyboard.TargetName="HistCardBg" Storyboard.TargetProperty="Color"
                                                To="#222222" Duration="0:0:0.2"/>
                                <ColorAnimation Storyboard.TargetName="HistCardBorder" Storyboard.TargetProperty="Color"
                                                To="#2C2C2C" Duration="0:0:0.2"/>
                            </Storyboard>
                        </BeginStoryboard>
                    </EventTrigger>
                </Border.Triggers>
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <!-- Top-right buttons + task number -->
                    <StackPanel Grid.Row="0" HorizontalAlignment="Right" VerticalAlignment="Top"
                                Margin="0,-2,-2,0" Panel.ZIndex="1">
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <!-- Verify button (visible when finished) -->
                        <Button Click="VerifyTask_Click" Margin="0,0,2,0" FontSize="11">
                            <Button.Style>
                                <Style TargetType="Button" BasedOn="{StaticResource IconBtn}">
                                    <Setter Property="Content" Value="&#xE721;"/>
                                    <Setter Property="ToolTip" Value="Run result verification"/>
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsFinished}" Value="True">
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                        </Button>
                        <!-- Commit/Uncommit button (visible when finished) -->
                        <Button Click="CommitTask_Click" Margin="0,0,2,0" FontSize="11">
                            <Button.Style>
                                <Style TargetType="Button" BasedOn="{StaticResource IconBtn}">
                                    <Setter Property="Content" Value="&#xE74B;"/>
                                    <Setter Property="ToolTip" Value="Commit task changes"/>
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsFinished}" Value="True">
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding IsCommitted}" Value="True">
                                            <Setter Property="Content" Value="&#xE74D;"/>
                                            <Setter Property="Foreground" Value="{StaticResource Success}"/>
                                            <Setter Property="ToolTip" Value="Uncommit task changes"/>
                                        </DataTrigger>
                                        <!-- Pulse animation when uncommitted -->
                                        <DataTrigger Binding="{Binding IsCompletedUncommitted}" Value="True">
                                            <DataTrigger.EnterActions>
                                                <BeginStoryboard x:Name="CommitButtonPulseStoryboard">
                                                    <Storyboard RepeatBehavior="Forever">
                                                        <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                                                         From="0.6" To="1" Duration="0:0:0.8"
                                                                         AutoReverse="True"/>
                                                    </Storyboard>
                                                </BeginStoryboard>
                                            </DataTrigger.EnterActions>
                                            <DataTrigger.ExitActions>
                                                <StopStoryboard BeginStoryboardName="CommitButtonPulseStoryboard"/>
                                            </DataTrigger.ExitActions>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                        </Button>
                        <!-- Close X button -->
                        <Button Content="&#xE8BB;" Click="RemoveHistoryTask_Click"
                                Style="{StaticResource IconBtn}"
                                ToolTip="Remove from history"/>
                    </StackPanel>
                    <TextBlock Text="{Binding TaskNumber, StringFormat='#{0:D4}'}"
                               Foreground="{StaticResource TextMuted}"
                               FontSize="10" FontFamily="Consolas" Margin="0,2,2,0"
                               HorizontalAlignment="Right"/>
                    </StackPanel>
                    <StackPanel Grid.Row="0" Margin="14,0,0,0">
                        <TextBlock Text="{Binding Id, StringFormat='ID: {0}'}"
                                   Foreground="{StaticResource TextMuted}"
                                   FontSize="9" FontFamily="Consolas" Margin="0,0,0,2"/>
                    <StackPanel Orientation="Horizontal">
                        <Grid Width="8" Height="8" Margin="0,0,6,0" VerticalAlignment="Center">
                            <Ellipse x:Name="HistStatusGlow" Width="8" Height="8"
                                     Fill="{Binding StatusColor, Converter={StaticResource StringToBrush}}">
                                <Ellipse.Style>
                                    <Style TargetType="Ellipse">
                                        <Setter Property="Effect">
                                            <Setter.Value>
                                                <BlurEffect Radius="0"/>
                                            </Setter.Value>
                                        </Setter>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsRunning}" Value="True">
                                                <DataTrigger.EnterActions>
                                                    <BeginStoryboard x:Name="HistGlowStoryboard">
                                                        <Storyboard RepeatBehavior="Forever">
                                                            <DoubleAnimation Storyboard.TargetProperty="(UIElement.Effect).(BlurEffect.Radius)"
                                                                             From="0" To="10" Duration="0:0:0.8"
                                                                             AutoReverse="True">
                                                                <DoubleAnimation.EasingFunction>
                                                                    <SineEase EasingMode="EaseInOut"/>
                                                                </DoubleAnimation.EasingFunction>
                                                            </DoubleAnimation>
                                                            <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                                                             From="0.3" To="1.0" Duration="0:0:0.8"
                                                                             AutoReverse="True">
                                                                <DoubleAnimation.EasingFunction>
                                                                    <SineEase EasingMode="EaseInOut"/>
                                                                </DoubleAnimation.EasingFunction>
                                                            </DoubleAnimation>
                                                        </Storyboard>
                                                    </BeginStoryboard>
                                                </DataTrigger.EnterActions>
                                                <DataTrigger.ExitActions>
                                                    <StopStoryboard BeginStoryboardName="HistGlowStoryboard"/>
                                                </DataTrigger.ExitActions>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding IsQueued}" Value="True">
                                                <DataTrigger.EnterActions>
                                                    <BeginStoryboard x:Name="HistQueuedGlowStoryboard">
                                                        <Storyboard RepeatBehavior="Forever">
                                                            <DoubleAnimation Storyboard.TargetProperty="(UIElement.Effect).(BlurEffect.Radius)"
                                                                             From="0" To="8" Duration="0:0:1.2"
                                                                             AutoReverse="True">
                                                                <DoubleAnimation.EasingFunction>
                                                                    <SineEase EasingMode="EaseInOut"/>
                                                                </DoubleAnimation.EasingFunction>
                                                            </DoubleAnimation>
                                                            <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                                                             From="0.4" To="1.0" Duration="0:0:1.2"
                                                                             AutoReverse="True">
                                                                <DoubleAnimation.EasingFunction>
                                                                    <SineEase EasingMode="EaseInOut"/>
                                                                </DoubleAnimation.EasingFunction>
                                                            </DoubleAnimation>
                                                        </Storyboard>
                                                    </BeginStoryboard>
                                                </DataTrigger.EnterActions>
                                                <DataTrigger.ExitActions>
                                                    <StopStoryboard BeginStoryboardName="HistQueuedGlowStoryboard"/>
                                                </DataTrigger.ExitActions>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding IsCompletedUncommitted}" Value="True">
                                                <DataTrigger.EnterActions>
                                                    <BeginStoryboard x:Name="HistUncommittedPulseStoryboard">
                                                        <Storyboard RepeatBehavior="Forever">
                                                            <DoubleAnimation Storyboard.TargetProperty="Effect.Radius"
                                                                             From="0" To="10" Duration="0:0:1"
                                                                             AutoReverse="True"/>
                                                            <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                                                             From="0.7" To="1" Duration="0:0:1"
                                                                             AutoReverse="True"/>
                                                        </Storyboard>
                                                    </BeginStoryboard>
                                                </DataTrigger.EnterActions>
                                                <DataTrigger.ExitActions>
                                                    <StopStoryboard BeginStoryboardName="HistUncommittedPulseStoryboard"/>
                                                </DataTrigger.ExitActions>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Ellipse.Style>
                            </Ellipse>
                            <Ellipse x:Name="HistStatusDot" Width="8" Height="8"
                                     Fill="{Binding StatusColor, Converter={StaticResource StringToBrush}}">
                                <Ellipse.Style>
                                    <Style TargetType="Ellipse">
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsCompletedUncommitted}" Value="True">
                                                <DataTrigger.EnterActions>
                                                    <BeginStoryboard x:Name="HistDotPulseStoryboard">
                                                        <Storyboard RepeatBehavior="Forever">
                                                            <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                                                             From="0.6" To="1" Duration="0:0:1.2"
                                                                             AutoReverse="True"/>
                                                        </Storyboard>
                                                    </BeginStoryboard>
                                                </DataTrigger.EnterActions>
                                                <DataTrigger.ExitActions>
                                                    <StopStoryboard BeginStoryboardName="HistDotPulseStoryboard"/>
                                                </DataTrigger.ExitActions>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Ellipse.Style>
                            </Ellipse>
                        </Grid>
                        <TextBlock Text="{Binding QueueStatusText}"
                                   Foreground="{Binding StatusColor, Converter={StaticResource StringToBrush}}"
                                   FontSize="11" FontWeight="SemiBold" FontFamily="Segoe UI"
                                   VerticalAlignment="Center">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Style.Triggers>
                                        <MultiDataTrigger>
                                            <MultiDataTrigger.Conditions>
                                                <Condition Binding="{Binding Path=IsQueued}" Value="True"/>
                                            </MultiDataTrigger.Conditions>
                                            <Setter Property="ToolTip">
                                                <Setter.Value>
                                                    <ToolTip MaxWidth="400" Placement="Top">
                                                        <TextBlock Text="{Binding QueuedTooltip}" TextWrapping="Wrap"/>
                                                    </ToolTip>
                                                </Setter.Value>
                                            </Setter>
                                        </MultiDataTrigger>
                                        <MultiDataTrigger>
                                            <MultiDataTrigger.Conditions>
                                                <Condition Binding="{Binding Path=IsInitQueued}" Value="True"/>
                                            </MultiDataTrigger.Conditions>
                                            <Setter Property="ToolTip">
                                                <Setter.Value>
                                                    <ToolTip MaxWidth="400" Placement="Top">
                                                        <TextBlock Text="{Binding QueuedTooltip}" TextWrapping="Wrap"/>
                                                    </ToolTip>
                                                </Setter.Value>
                                            </Setter>
                                        </MultiDataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                        <TextBlock Text=" | " Foreground="{StaticResource SeparatorDark}" FontSize="11" FontFamily="Segoe UI"
                                   VerticalAlignment="Center"/>
                        <TextBlock Text="{Binding ProjectName}" Foreground="{Binding ProjectColor, Converter={StaticResource StringToBrush}}"
                                   FontSize="11" FontFamily="Segoe UI" VerticalAlignment="Center"
                                   TextTrimming="CharacterEllipsis" MaxWidth="100"
                                   ToolTip="{Binding ProjectPath}"/>
                    </StackPanel>
                    </StackPanel>
                    <TextBlock Grid.Row="1" Text="{Binding ShortDescription}"
                               Foreground="{StaticResource TextSubdued}" FontSize="12" FontFamily="Segoe UI"
                               TextTrimming="CharacterEllipsis" Margin="0,4,0,0">
                        <TextBlock.ToolTip>
                            <ToolTip MaxWidth="600" Placement="Bottom">
                                <TextBlock Text="{Binding Description}" TextWrapping="Wrap" MaxWidth="580"/>
                            </ToolTip>
                        </TextBlock.ToolTip>
                    </TextBlock>
                    <!-- Recommendations / Next Steps -->
                    <TextBlock Grid.Row="2" Text="{Binding Recommendations}"
                               Foreground="#80CBC4" FontSize="10" FontFamily="Segoe UI"
                               TextWrapping="Wrap" TextTrimming="CharacterEllipsis"
                               MaxHeight="40" Margin="0,4,0,0">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Setter Property="Visibility" Value="Collapsed"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding HasRecommendations}" Value="True">
                                        <Setter Property="Visibility" Value="Visible"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                        <TextBlock.ToolTip>
                            <ToolTip MaxWidth="600" Placement="Bottom">
                                <TextBlock Text="{Binding Recommendations}" TextWrapping="Wrap" MaxWidth="580"
                                           Foreground="#B2DFDB"/>
                            </ToolTip>
                        </TextBlock.ToolTip>
                    </TextBlock>
                    <Grid Grid.Row="3" Margin="0,6,0,0">
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <TextBlock Text="{Binding TimeInfo}" Foreground="{StaticResource TextDisabled}"
                                       FontSize="10" FontFamily="Segoe UI"/>
                            <TextBlock Text=" | " Foreground="{StaticResource SeparatorDark}" FontSize="10" FontFamily="Segoe UI">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding HasTokenData}" Value="True">
                                                <Setter Property="Visibility" Value="Visible"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                            <TextBlock Text="{Binding TokenDisplayText}" Foreground="#8899AA"
                                       FontSize="10" FontFamily="Consolas">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding HasTokenData}" Value="True">
                                                <Setter Property="Visibility" Value="Visible"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                            <!-- Per-task toggle badges -->
                            <Border CornerRadius="3" Padding="4,1" Margin="0,0,4,0"
                                    Background="#33607D8B" BorderBrush="#55607D8B" BorderThickness="1"
                                    VerticalAlignment="Center"
                                    ToolTip="{Binding ActiveTogglesTooltip}">
                                <Border.Style>
                                    <Style TargetType="Border">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding HasActiveToggles}" Value="True">
                                                <Setter Property="Visibility" Value="Visible"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>
                                <TextBlock Text="{Binding ActiveTogglesText}" Foreground="#90A4AE" FontSize="9"
                                           FontFamily="Segoe UI" FontWeight="Bold"/>
                            </Border>
                            <Border CornerRadius="3" Padding="4,1" Margin="0,0,8,0"
                                    Background="{StaticResource PlanBadgeBg}" BorderBrush="{StaticResource PlanBadgeBorder}" BorderThickness="1"
                                    VerticalAlignment="Center">
                                <Border.Style>
                                    <Style TargetType="Border">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding PlanOnly}" Value="True">
                                                <Setter Property="Visibility" Value="Visible"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>
                                <TextBlock Text="PLAN" Foreground="{StaticResource PlanBadgeText}" FontSize="9"
                                           FontFamily="Segoe UI" FontWeight="Bold"/>
                            </Border>
                            <Button Content="Retry" Click="RetryTask_Click"
                                    Background="#E07840"
                                    Margin="0,0,4,0">
                                <Button.Style>
                                    <Style TargetType="Button" BasedOn="{StaticResource SmallBtn}">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsRetryable}" Value="True">
                                                <Setter Property="Visibility" Value="Visible"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Button.Style>
                            </Button>
                            <Button Content="Continue" Click="ContinueTask_Click"
                                    Background="#FFB74D"
                                    Foreground="#1A1A1A"
                                    Margin="0,0,4,0"
                                    ToolTip="Continue task with recommended next steps">
                                <Button.Style>
                                    <Style TargetType="Button" BasedOn="{StaticResource SmallBtn}">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsContinuable}" Value="True">
                                                <Setter Property="Visibility" Value="Visible"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Button.Style>
                            </Button>
                        </StackPanel>
                    </Grid>
                </Grid>
            </Border>
        </DataTemplate>

        <!-- ═══ STORED TASK ═══ -->
        <DataTemplate x:Key="StoredTaskTemplate">
            <Border CornerRadius="8" Padding="10" Margin="0,2"
                    BorderThickness="1"
                    >
                <Border.ContextMenu>
                    <ContextMenu>
                        <MenuItem Header="Copy Stored Prompt" Click="CopyStoredPrompt_Click"/>
                        <MenuItem Header="Copy Original Prompt" Click="CopyPrompt_Click"/>
                        <Separator/>
                        <MenuItem Header="Export Output" Click="ExportTask_Click"/>
                    </ContextMenu>
                </Border.ContextMenu>
                <Border.Background>
                    <SolidColorBrush x:Name="StoredCardBg" Color="#1E2428"/>
                </Border.Background>
                <Border.BorderBrush>
                    <SolidColorBrush x:Name="StoredCardBorder" Color="#2A3A3A"/>
                </Border.BorderBrush>
                <Border.Triggers>
                    <EventTrigger RoutedEvent="MouseEnter">
                        <BeginStoryboard>
                            <Storyboard>
                                <ColorAnimation Storyboard.TargetName="StoredCardBg" Storyboard.TargetProperty="Color"
                                                To="#253030" Duration="0:0:0.15"/>
                                <ColorAnimation Storyboard.TargetName="StoredCardBorder" Storyboard.TargetProperty="Color"
                                                To="#3A4A4A" Duration="0:0:0.15"/>
                            </Storyboard>
                        </BeginStoryboard>
                    </EventTrigger>
                    <EventTrigger RoutedEvent="MouseLeave">
                        <BeginStoryboard>
                            <Storyboard>
                                <ColorAnimation Storyboard.TargetName="StoredCardBg" Storyboard.TargetProperty="Color"
                                                To="#1E2428" Duration="0:0:0.2"/>
                                <ColorAnimation Storyboard.TargetName="StoredCardBorder" Storyboard.TargetProperty="Color"
                                                To="#2A3A3A" Duration="0:0:0.2"/>
                            </Storyboard>
                        </BeginStoryboard>
                    </EventTrigger>
                </Border.Triggers>
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <!-- Close X button + task number top-right -->
                    <StackPanel Grid.Row="0" HorizontalAlignment="Right" VerticalAlignment="Top"
                                Margin="0,-2,-2,0">
                        <Button Content="&#xE8BB;" Click="RemoveStoredTask_Click"
                                Style="{StaticResource IconBtn}" HorizontalAlignment="Right"
                                ToolTip="Remove stored task"/>
                        <TextBlock Text="{Binding TaskNumber, StringFormat='#{0:D4}'}"
                                   Foreground="{StaticResource TextMuted}"
                                   FontSize="10" FontFamily="Consolas" Margin="0,2,2,0"
                                   HorizontalAlignment="Right"/>
                    </StackPanel>
                    <StackPanel Grid.Row="0">
                        <TextBlock Text="{Binding Id, StringFormat='ID: {0}'}"
                                   Foreground="{StaticResource TextMuted}"
                                   FontSize="9" FontFamily="Consolas" Margin="0,0,0,2"/>
                        <StackPanel Orientation="Horizontal">
                            <Grid Width="8" Height="8" Margin="0,0,6,0" VerticalAlignment="Center">
                                <Ellipse Width="8" Height="8" Fill="{StaticResource AccentTeal}"/>
                            </Grid>
                            <TextBlock Text="Stored" Foreground="{StaticResource AccentTeal}"
                                       FontSize="11" FontWeight="SemiBold" FontFamily="Segoe UI"
                                       VerticalAlignment="Center"/>
                            <TextBlock Text=" | " Foreground="{StaticResource SeparatorDark}" FontSize="11" FontFamily="Segoe UI"
                                       VerticalAlignment="Center"/>
                            <TextBlock Text="{Binding ProjectName}" Foreground="{Binding ProjectColor, Converter={StaticResource StringToBrush}}"
                                       FontSize="11" FontFamily="Segoe UI" VerticalAlignment="Center"
                                       TextTrimming="CharacterEllipsis" MaxWidth="100"
                                       ToolTip="{Binding ProjectPath}"/>
                        </StackPanel>
                    </StackPanel>
                    <TextBlock Grid.Row="1" Text="{Binding ShortDescription}"
                               Foreground="{StaticResource TextSubdued}" FontSize="12" FontFamily="Segoe UI"
                               TextTrimming="CharacterEllipsis" Margin="0,4,0,0">
                        <TextBlock.ToolTip>
                            <ToolTip MaxWidth="600" Placement="Bottom">
                                <TextBlock Text="{Binding StoredPrompt}" TextWrapping="Wrap" MaxWidth="580"/>
                            </ToolTip>
                        </TextBlock.ToolTip>
                    </TextBlock>
                    <Grid Grid.Row="2" Margin="0,6,0,0">
                        <TextBlock Text="{Binding TimeInfo}" Foreground="{StaticResource TextDisabled}"
                                   FontSize="10" VerticalAlignment="Center" FontFamily="Segoe UI"/>
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                            <Button Content="View" Click="ViewStoredTask_Click"
                                    Background="{StaticResource BorderSubtle}" Style="{StaticResource SmallBtn}"
                                    Margin="0,0,6,0"/>
                            <Button Content="Execute" Click="ExecuteStoredTask_Click"
                                    Background="{StaticResource AccentTeal}" Style="{StaticResource SmallBtn}"
                                    Margin="0"/>
                        </StackPanel>
                    </Grid>
                </Grid>
            </Border>
        </DataTemplate>
        <!-- ═══ SAVED PROMPT CARD ═══ -->
        <DataTemplate x:Key="SavedPromptTemplate">
            <Border CornerRadius="10" Padding="8,3,4,3" Margin="0,0,4,4" Cursor="Hand"
                    MaxWidth="220"
                    MouseLeftButtonDown="SavedPromptCard_MouseLeftButtonDown"
                    MouseDown="SavedPromptCard_MouseDown">
                <Border.Style>
                    <Style TargetType="Border">
                        <Setter Property="Background" Value="{StaticResource BgPopup}"/>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{StaticResource BgCardHover}"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Border.Style>
                <Border.ContextMenu>
                    <ContextMenu>
                        <MenuItem Header="Run" Click="SavedPromptRun_Click"/>
                        <MenuItem Header="Copy Prompt" Click="SavedPromptCopy_Click"/>
                    </ContextMenu>
                </Border.ContextMenu>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="{Binding DisplayName}"
                               Foreground="{StaticResource TextLight}" FontSize="11"
                               FontFamily="Segoe UI" TextTrimming="CharacterEllipsis"
                               MaxWidth="180" VerticalAlignment="Center"
                               ToolTip="{Binding PromptText, Converter={StaticResource WrapTooltipText}}"/>
                    <Button Content="&#xE711;" FontSize="9"
                            Click="DeleteSavedPrompt_Click"
                            Padding="4,2" VerticalAlignment="Center" Margin="2,0,0,0">
                        <Button.Style>
                            <Style TargetType="Button" BasedOn="{StaticResource IconBtn}">
                                <Setter Property="Foreground" Value="{StaticResource TextSubdued}"/>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Foreground" Value="{StaticResource DangerDeleteHover}"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                </StackPanel>
            </Border>
        </DataTemplate>


</ResourceDictionary>
